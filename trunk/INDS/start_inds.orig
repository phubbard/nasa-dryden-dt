#! /bin/csh

set is_running=`ps axww | grep java | grep INDS`
echo `date` > start_inds.log

if(${#is_running} == 0) then
    echo "Starting INDS" >> start_inds.log
    echo "Starting INDS..."
    cd /home/suborbital/INDS
    source ./environment
###    ./start_tomcat
    ./startup.sh
    echo "INDS launched." >> start_inds.log
else
    echo "INDS already running" >> start_inds.log
endif

